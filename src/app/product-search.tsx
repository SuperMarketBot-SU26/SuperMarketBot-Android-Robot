// Kiosk search is deliberately public: guests can search and request guidance.
// The screen itself switches to personalized search only when a member token exists.
import MemberSearchScreen from '../components/screens/MemberSearchScreen';
export default MemberSearchScreen;
