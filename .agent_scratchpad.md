# Agent Scratchpad

## Current Goal
Chuyển đổi toàn bộ ngôn ngữ mục **3.2 App for Robot** sang tiếng Anh kỹ thuật (English), giữ nguyên các tên nút bấm/thành phần UI riêng biệt bằng tiếng Việt đặt trong dấu ngoặc kép.

## Progress / Completed Steps
- [x] Phân tích nguyên nhân giật lag của app Android Robot.
- [x] Đổi tên chuẩn hóa toàn bộ 16 ảnh chụp màn hình trong `screenShot/` theo User Flow.
- [x] Tạo bản sao lưu an toàn `Report3_Software Requirement Specification_BACKUP.docx`.
- [x] Khắc phục lỗi tiến trình WINWORD.EXE khóa tệp docx trên Windows (giải phóng file lock an toàn).
- [x] Cập nhật toàn bộ nội dung 8 phân hệ (3.2.1 - 3.2.8) sang tiếng Anh kỹ thuật:
  - 100% các trường: Function Trigger, Timing / Frequency, Function Description, Normal Flow, Abnormal Cases, Data Processing, Business Rules.
  - Giữ nguyên các nút bấm UI, thẻ, nhãn và thông báo hệ thống đặc thù bằng tiếng Việt trong dấu ngoặc kép (ví dụ: `"QUÉT FACE ID ĐĂNG NHẬP"`, `"Khách hàng thành viên"`, `"Khuyến mãi hôm nay"`, `"Dành riêng cho bạn"`, `"Robot dẫn theo giỏ hàng"`).
  - 16/16 ảnh thực tế nhúng thành công.
  - 12/12 cờ màn hình tương lai được đánh dấu `[TODO: ADD SCREENSHOT - ...]` bằng màu đỏ in đậm `#DC2626`.
- [x] Xác thực toàn vẹn tài liệu với `tools/verify_patched_srs.py`:
  - 3.696 đoạn văn bản, 11 bảng nguyên vẹn.
  - Mục lục 3.2.1 - 3.2.8 hiển thị tuần tự, chuẩn xác.

## Key Decisions Made
1. **Ngôn ngữ chuẩn**: 100% tiếng Anh kỹ thuật chuyên nghiệp cho toàn bộ phần mô tả và luồng người dùng; tên nút bấm (buttons), nhãn giao diện (UI labels) và thông báo (toasts) gốc giữ bằng tiếng Việt trong dấu ngoặc kép.
2. **Định dạng TODO**: `[TODO: ADD SCREENSHOT - ...]` viết bằng tiếng Anh, màu đỏ `#DC2626` in đậm (`bold=True`).

## Remaining TODOs
- [x] Cập nhật Walkthrough artifact và báo cáo cho người dùng.
